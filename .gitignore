/target
marathon-formats/target
tests/fixtures/

# dev-ops worktrees (created/torn down by the orchestrator; often root-owned)
.worktrees/

# transient dev-ops visual-verification artifacts (root-level scratch screenshots
# and Playwright MCP logs). Scoped narrowly so e2e/ visual-regression baselines
# stay trackable.
/marathon-*.png
/s7-*.png
.playwright-mcp/
/logs/
# local human-gated GPU verification workspace (scripts + per-run evidence);
# the box 4.3 write-up is committed under openspec/.../evidence/, not here
/scratch/
# e2e local install + Playwright run artifacts (traces, screenshots, reports)
/e2e/node_modules/
/e2e/test-results/
/e2e/playwright-report/
/e2e/investigation/evidence/
*.sceA
*.shpA
*.sndA
*.phyA
*.sgaA
*.filA
